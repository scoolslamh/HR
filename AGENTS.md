# تعليمات تطوير مشروع بوابة الحضور والانصراف

## وصف المشروع

نظام ويب مؤسسي عربي لإدارة وتحليل حضور وانصراف الموظفين.

يتم رفع ملف Excel أسبوعيًا، وكل صف يمثل سجل حضور يومي لموظف.

المعرف المعتمد لمطابقة الموظفين هو السجل المدني، لكن السجل المدني ليس المفتاح الأساسي في قاعدة البيانات.

## التقنية المعتمدة

- Python 3.13
- Django 5.2 LTS
- PostgreSQL للإنتاج
- SQLite للتطوير المحلي فقط
- Django Templates
- Bootstrap 5 RTL
- Pandas
- OpenPyXL
- Pytest

## قواعد اللغة والواجهة

- جميع الواجهات باللغة العربية.
- اتجاه الصفحات RTL.
- المنطقة الزمنية Asia/Riyadh.
- التصميم متجاوب مع الكمبيوتر والجوال.
- النصوص البرمجية وأسماء المتغيرات والجداول باللغة الإنجليزية.
- رسائل المستخدم واضحة ومختصرة باللغة العربية.

## قواعد قاعدة البيانات

- استخدام UUID كمفتاح أساسي لجميع الجداول.
- إنشاء Custom User Model من بداية المشروع.
- تسجيل الدخول باسم المستخدم وكلمة المرور.
- السجل المدني فريد بعد التطبيع والتحقق.
- لا يستخدم السجل المدني في الروابط أو أسماء الملفات.
- لا يجوز حذف البيانات التاريخية المهمة حذفًا نهائيًا.
- تستخدم الأرشفة أو Soft Delete عند الحاجة.
- تضاف الفهارس للأعمدة كثيرة الاستخدام.
- تستخدم معاملات قاعدة البيانات في عمليات الاستيراد والاعتماد.

## قواعد العمل الأساسية

1. مطابقة الموظف تتم بالسجل المدني وليس بالاسم.
2. البيانات الأصلية المستوردة من Excel لا تعدل.
3. أي معالجة إدارية تحفظ في سجل مستقل.
4. مسؤول القسم يشاهد موظفي قسمه فقط.
5. مدير النظام يشاهد جميع الأقسام وفق صلاحياته.
6. الصلاحيات تفرض من الخادم، وليس من الواجهة فقط.
7. كل تعديل أو اعتماد أو معالجة يسجل في Audit Log.
8. يمنع استيراد الملف أو السجل نفسه أكثر من مرة.
9. يجب التحقق من نوع الملف وحجمه وأعمدته ومحتواه.
10. لا تعتمد المخالفة المعدلة إلا بعد اكتمال مسار الاعتماد المطلوب.
11. لا يجوز للمستخدم اعتماد طلب أنشأه لنفسه.
12. يحتفظ النظام بتاريخ الأقسام والمواقع والإسنادات الوظيفية.
13. يقارن مكان توقيع الموظف بموقعه الأساسي النافذ في تاريخ الحضور.

## قواعد تنظيم الكود

- لا تضع منطق الأعمال داخل Views.
- لا تضع منطق الاستيراد أو التحليل المعقد داخل Models.
- استخدم طبقة Services لمنطق الأعمال.
- استخدم Selectors أو Query Services للاستعلامات المركبة عند الحاجة.
- استخدم Forms أو Serializers للتحقق من المدخلات.
- استخدم type hints حيثما أمكن.
- اجعل الدوال والفئات صغيرة وواضحة المسؤولية.
- لا تكرر منطق الأعمال بين التطبيقات.
- لا تنفذ تغييرات واسعة خارج نطاق المهمة المطلوبة.

## الأمان

- لا تحفظ الأسرار أو كلمات المرور داخل الكود.
- استخدم متغيرات البيئة.
- تحقق من الصلاحيات والنطاق التنظيمي في كل عملية.
- لا تسجل كلمات المرور أو الأسرار أو السجل المدني كاملًا في Logs.
- تحقق من ملفات Excel والمرفقات قبل الحفظ.
- استخدم CSRF وحماية Django الافتراضية.
- لا تعرض تفاصيل الأخطاء الداخلية للمستخدم النهائي.
- استخدم HTTPS في بيئة الإنتاج.

## الاختبارات والجودة

- أضف اختبارات لكل ميزة جديدة.
- اختبر الصلاحيات والنطاق التنظيمي.
- اختبر منع التكرار.
- اختبر عمليات الاستيراد والمعالجة داخل Transactions.
- شغّل بعد كل مرحلة:
  - python manage.py check
  - python manage.py test
- لا تعتبر المهمة مكتملة إذا فشلت الاختبارات.

## قواعد الترحيلات

- لا تنشئ Migration إلا عندما تطلب المهمة ذلك.
- لا تحذف Migration موجودة دون سبب موثق.
- لا تعدل Migration مطبقة في بيئة مشتركة.
- اعرض ملخصًا لأي تغيير في قاعدة البيانات.

## طريقة تنفيذ المهام

قبل أي مهمة:

1. اقرأ AGENTS.md.
2. اقرأ الأجزاء ذات الصلة من PROJECT_PLAN.md.
3. افحص الملفات الحالية قبل التعديل.
4. اذكر الخطة بإيجاز.
5. نفذ المرحلة المطلوبة فقط.
6. شغّل الفحوص والاختبارات المناسبة.
7. اعرض ملخص الملفات المعدلة والنتائج.
8. توقف ولا تبدأ مرحلة أخرى تلقائيًا.