# محرك احتساب الحضور V1

## المنجز

- إضافة `CalculationRun` لتوثيق كل تشغيل احتساب أو إعادة احتساب.
- إضافة `DailyAttendanceResult` لحفظ النتيجة اليومية القابلة للإصدار.
- احتساب حالة الحضور، ساعات العمل، النقص، التأخر، الانصراف المبكر، الحضور المبكر والعمل الإضافي.
- مقارنة مكان الحضور ومكان الانصراف كلٌ على حدة بالموقع الأساسي النافذ للموظف.
- إضافة تقرير «التوقيع خارج المكان المعتمد».
- إضافة شاشة سجل الحضور اليومي ومرشحات الموظف والقسم والفترة.
- إضافة لوحة تقارير أولية ومؤشرات مجمعة.
- تشغيل الاحتساب تلقائيًا عند اعتماد دفعة حضور جديدة.
- إضافة أمر `calculate_attendance` لمعالجة السجلات القديمة.

## تشغيل المحرك على السجلات الحالية

```powershell
python manage.py migrate
python manage.py calculate_attendance --all
python manage.py runserver
```

## ملاحظة احتساب التأخر

في الإصدار V1، وقبل بناء سياسات المناوبات الرسمية، تُحتسب دقائق التأخر من الفرق المتبقي بين نقص الدوام والانصراف المبكر. ستُستبدل هذه القاعدة لاحقًا بمحرك السياسات المعتمد.
